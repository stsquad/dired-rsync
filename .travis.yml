language: minimal
sudo: required
dist: trusty
os:
  - linux
  - osx
addons:
  homebrew:
    packages:
      - autoconf
      - automake
      - gnutls
      - texinfo
cache:
- directories:
  - "$HOME/emacs"
matrix:
  fast_finish: true
  allow_failures:
  - env: EMACS_VERSION=snapshot
env:
  global:
    - EMAKE_SHA1=2f22d5b272443246e4b4c4b3a23533be78655f2e
    - EMACS_DIR=$HOME/emacs
    - EMACS=$HOME/emacs/src/emacs
  matrix:
    - EMACS_VERSION=25.1
    - EMACS_VERSION=25.2
    - EMACS_VERSION=25.3
    - EMACS_VERSION=26.1
    - EMACS_VERSION=snapshot
before_install:
  - sudo rm -rf "$HOME/emacs"
  - make setup
  - wget "https://raw.githubusercontent.com/vermiculus/emake.el/${EMAKE_SHA1}/emake.mk"
install:
  - make install
script:
  - time make travis-script
